version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dke_gruppe_zwei
      POSTGRES_PASSWORD: pr_dke_foerderportal_2025
      POSTGRES_DB: pr_dke_project
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres-data: